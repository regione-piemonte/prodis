Comunicazione:
  type: object
  properties:
    codComunicazione:
      type: string
    dataFine:
      type: string
      format: date-time
    dataInizio:
      type: string
      format: date-time
    dataTmst:
      type: string
      format: date-time
    descComunicazione:
      type: string
    id:
      type: integer
    prospettos:
      type: array
      items:  
        $ref: '#/definitions/Prospetto'
